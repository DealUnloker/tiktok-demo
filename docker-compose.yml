services:
  web:
    build:
      context: .
    ports:
      - '3000:3000'
    # .env* files are dockerignored, so env is provided at runtime here.
    # Optional: cp .env.example .env.local to customize SITE_URL
    env_file:
      - path: .env.local
        required: false
    restart: unless-stopped
