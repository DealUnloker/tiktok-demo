# Public site origin for robots.txt / sitemap.xml (optional, defaults to
# http://localhost:3000). Those routes are force-dynamic, so this is read at
# runtime — pass it to the container when deploying.
#SITE_URL=https://example.com
